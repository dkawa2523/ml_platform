# Compatibility file for legacy pip-based local setup.
# Source of truth is pyproject.toml dependency groups plus uv.lock.
-r requirements.txt
pytest>=8.0
ruff>=0.8
pre-commit>=4.0
gitlint>=0.19
radon>=6.0
import-linter>=2.0
