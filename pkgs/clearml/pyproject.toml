[project]
name = "ml-platform-clearml"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "ml-platform-core",
  "ml-platform-tabular",
]

[project.optional-dependencies]
sdk = ["clearml==2.1.7"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
