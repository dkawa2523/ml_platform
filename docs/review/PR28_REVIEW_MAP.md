# PR #28 review response map

この表は、PR #28相当のレビューコメントR01〜R27を、現在の `ml_platform` リポジトリで追跡するための対応表です。

## Status values

- `todo`: 未着手
- `in_progress`: 作業中
- `done`: 対応済み
- `blocked`: 外部情報・権限・環境待ち
- `needs_confirmation`: 元設定や運用条件の確認が必要
- `not_applicable`: 現repoでは対象外
- `deferred`: 別Phaseへ延期

## Prompt 0-A baseline notes

- R01: Static tooling is not available in the current `.venv`; `ruff`, `mypy`, `pre_commit`, `gitlint`, `radon`, and `lint_imports` are missing. `requirements-dev.txt` only includes pytest through `requirements.txt`.
- R02: `uv` is available, but `uv.lock` is absent. `uv sync --all-extras --dev --dry-run` would create a lockfile and change many installed packages, so actual sync is deferred.
- R03: BLAS/OpenMP thread environment defaults are currently set in `clearml/app.py`, `scripts/local_run.py`, and `scripts/make_sample_data.py`.
- R15: `ui_params`, `ui_value`, `default_ui_params`, and `pipeline_ui_params` remain in ClearML runtime files and mapping tests. Rename/migration should stay compatibility-focused.
- R17: Manual `sys.path` bootstrap exists in `clearml/_entrypoint_bootstrap.py` and `scripts/_bootstrap.py`.
- R19: `TABLE_SUFFIXES` exists in `pkgs/core/src/ml_platform_core/io.py` and is currently used for recursive table discovery.
- R20: `set_dotted_path = set_by_dotted_path` remains in `pkgs/core/src/ml_platform_core/config.py`; usage/removal requires confirmation by search and compatibility checks.
- R21: `.pre-commit-config.yaml` and `.gitlint` are absent. Original intended settings are not recoverable from the current tree alone.
- R22: `.gitattributes` is absent. Original intended settings are not recoverable from the current tree alone.
- R23: Only `.github/workflows/ci.yml` is tracked; MkDocs deploy workflow is absent. Deployment target/settings require confirmation before restoration.
- R24: `.vscode/` is absent. Shared VS Code settings/extensions require confirmation before restoration.
- R25: No `*.code-workspace` file is tracked. Intended workspace layout requires confirmation before restoration.
- R27: `Registry` exists in `pkgs/core/src/ml_platform_core/registry.py`; implementation/use decision is deferred to the relevant review branch.

## Prompt 1-A tooling/CI investigation notes

- Current branch: `review/r01-tooling-ci`; working tree was clean before recording this investigation.
- History candidates found: `.github/workflows/ci.yml` at `647bcdf` and `6637119`.
- No history candidates found in this repository for `.github/workflows/smoke-test.yml`, `.github/workflows/deploy-mkdocs.yml`, `.pre-commit-config.yaml`, `.gitlint`, `.gitattributes`, `.vscode/*`, or `*.code-workspace`.
- Current `.github/workflows/ci.yml` still uses `ubuntu-latest` and calls removed task configs: `tabular_train.yaml`, `tabular_eval.yaml`, and `tabular_1d_output.yaml`.
- Current task configs are `tabular_pipeline.yaml`, `tabular_stage.yaml`, and `tabular_infer.yaml`.
- Review source mentions runner set `arc-runner-set-spdml-ml-pipeline`, but availability in this repository cannot be confirmed locally.
- Prompt 1-B should restore a minimal common CI, split smoke workflow, and add minimal local developer tooling without doing the R02 uv migration.

## Prompt 1-B Phase 1 status override

The table below records the current Phase 1 status for tooling/CI rows. It is
the working override for R01/R13/R14/R21/R22/R23/R24/R25 until the main table is
normalized.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R01 | in_progress | `pyproject.toml`, `requirements-dev.txt`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml` | `review/r01-tooling-ci` | Restored Ruff, pre-commit, gitlint, radon, and import-linter configuration. `ty` was not added because availability/policy still needs confirmation. Ruff now runs and exposes existing issues instead of being absent. | `compileall` passed; `pytest` passed 89 tests; `ruff check` failed on existing E402/F841/F401; `ruff format --check` found 20 files needing format; `radon` ran with average B; `lint-imports` CLI passed. | pending commit | Port tooling config with the same commit. Keep `ty` decision separate from this compatibility restoration. | draft: static analysis tooling has been restored; remaining Ruff findings are tracked for later review IDs. |
| R13 | needs_confirmation | `.github/workflows/ci.yml`, `.github/workflows/smoke-test.yml`, `.github/workflows/deploy-mkdocs.yml` | `review/r01-tooling-ci` | Workflows keep `runs-on: ubuntu-latest` with TODO comments. Review-source runner `arc-runner-set-spdml-ml-pipeline` cannot be confirmed locally. | Local GitHub runner availability cannot be tested. | pending commit | Confirm runner label in target repo/org before changing `runs-on`. | draft: runner-set name is preserved as a TODO because local repo state cannot prove availability. |
| R14 | done | `.github/workflows/ci.yml`, `.github/workflows/smoke-test.yml`, `config/tasks/*.yaml` | `review/r01-tooling-ci` | Restored common package CI and split smoke workflow. Smoke workflow uses only existing `tabular_pipeline.yaml` and `tabular_infer.yaml`; removed task configs are no longer referenced. | Workflow syntax inspected; local `compileall` and `pytest` passed. CI lint steps intentionally expose current Ruff debt. | pending commit | Cherry-pick with R01/R13 notes because runner and lint baseline may differ in target repo. | draft: common CI and product smoke workflow are separated, and old removed tasks are not used. |
| R21 | done | `.pre-commit-config.yaml`, `.gitlint`, `requirements-dev.txt`, `pyproject.toml` | `review/r01-tooling-ci` | Added minimal pre-commit and gitlint config. YAML, TOML, EOF, whitespace hooks pass after excluding authoritative source/historical docs; Ruff hooks run when `.venv\Scripts` is on PATH and fail only on existing Ruff findings. | `pre_commit run --all-files` without activated `.venv` fails because `ruff` is not on PATH; with `.venv\Scripts` on PATH, basic hooks pass and Ruff hooks fail on existing code debt. `gitlint.exe --version` passes. | pending commit | Ensure target dev workflow activates its project environment before running local hooks. | draft: pre-commit/gitlint are restored with minimal non-secret config; existing Ruff debt remains tracked. |
| R22 | done | `.gitattributes` | `review/r01-tooling-ci` | Added text normalization, Windows script CRLF rules, and binary artifact protections. | `git status` confirms only intended files remain modified after reverting pre-commit's out-of-scope source/historical whitespace changes. | pending commit | Re-check line-ending churn after cherry-pick before committing in target repo. | draft: `.gitattributes` restored with conservative newline and binary rules. |
| R23 | needs_confirmation | `.github/workflows/deploy-mkdocs.yml`, `docs/ml_platform_mkdocs/mkdocs.yml`, `docs/ml_platform_mkdocs/requirements-docs.txt` | `review/r01-tooling-ci` | Added MkDocs build workflow and GitHub Pages deploy job for `main` only. Pages target/environment availability is not locally confirmable. | New workflow YAML passed targeted `check-yaml`; local MkDocs build was not run in this phase. GitHub Pages remains manual verification required. | pending commit | Confirm Pages settings/environment in target repo before relying on deploy job. | draft: MkDocs build/deploy workflow is restored, with deploy target confirmation still required. |
| R24 | done | `.vscode/extensions.json`, `.vscode/settings.json` | `review/r01-tooling-ci` | Added shared VS Code recommendations/settings without personal paths or secrets. | JSON passed targeted `check-json`; no absolute paths or secrets. | pending commit | Target repo may adjust team extension recommendations, keeping no personal paths/secrets. | draft: shared VS Code settings were restored as minimal team-safe defaults. |
| R25 | done | `ml_platform.code-workspace` | `review/r01-tooling-ci` | Added workspace with repo root, `pkgs/core`, and `pkgs/tabular` using relative paths only. | JSON passed targeted `check-json`; no absolute paths or secrets. | pending commit | Adjust folder names only if target repo layout differs. | draft: package workspace was restored with relative paths for root/core/tabular. |

## Prompt 2-A Phase 2 investigation status override

The table below records the current Phase 2 investigation status for
dependency, import, and runtime-entrypoint rows. It is the working override for
R02/R08/R16/R17 until the main table is normalized.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R02 | in_progress | `pyproject.toml`, `uv.lock`, `requirements.txt`, `requirements-dev.txt`, `pkgs/core/pyproject.toml`, `pkgs/tabular/pyproject.toml`, `.github/workflows/*`, `deploy/base/Dockerfile`, `clearml/templates.py` | `review/r02-dependency-import-runtime` | Root `pyproject.toml` has no runtime dependencies, while actual dependencies are split across requirements files and package pyprojects. `uv.lock` is absent. `requirements.txt` is still consumed by Docker/ClearML remote paths, so Prompt 2-B should make pyproject/uv the source of truth while keeping requirements as compatibility files. | `.venv` `compileall` passed; `.venv` `pytest` passed 89 tests. `uv sync --all-extras --dev --dry-run` would create `uv.lock` and change the environment; `uv sync --all-extras --dev --check` fails because the environment is outdated. Actual `uv sync` was not run. | pending commit | Port lockfile/dependency changes as an isolated commit. Confirm Docker and ClearML remote package installation still consume compatibility requirements safely in the target repo. | draft: dependency management will move to pyproject/uv/lock while requirements remain compatibility outputs for Docker and ClearML remote execution. |
| R08 | in_progress | `clearml/adapter.py`, `clearml/_entrypoint_bootstrap.py`, `clearml/app.py`, `clearml/pipelines.py`, `clearml/templates.py`, `scripts/_bootstrap.py` | `review/r02-dependency-import-runtime` | Current `.venv` resolves `import clearml` to the official SDK, but the local `clearml/` operations directory remains a shadow risk. `adapter.import_clearml_sdk()` still uses a dynamic import helper with temporary `sys.path` filtering. Do not rename `clearml/` in this phase because synced templates still execute `clearml/app.py` and `clearml/pipelines.py` directly. | Import probe succeeded for `ml_platform_core`, `ml_platform_tabular`, and official ClearML SDK. Search found dynamic import and shadow-guard logic in `clearml/adapter.py`. | pending commit | Keep path-compatible entrypoints for ClearML templates during porting. A future directory rename needs replacement entrypoints and remote template verification first. | draft: ClearML SDK import handling is being normalized without immediately renaming the runtime directory that remote templates still reference. |
| R16 | in_progress | `clearml/app.py`, `clearml/pipelines.py`, `clearml/templates.py`, `scripts/local_run.py`, `scripts/_bootstrap.py`, `clearml/_entrypoint_bootstrap.py` | `review/r02-dependency-import-runtime` | Imports cannot be moved to file tops while entrypoints depend on manual bootstrap before sibling/package imports. Prompt 2-B should reduce bootstrap reliance first, then move imports to the top where safe. Avoid broad formatting in this phase. | Ruff now runs and reports E402 in ClearML/script bootstrap entrypoints, plus F841 in `infer.py` and F401 in `pipeline.py`. `ruff format --check` reports 20 files would be reformatted. | pending commit | Apply import-order fixes separately from mass formatting to keep patches portable and reviewable. | draft: Ruff E402 findings are confirmed and tied to the current bootstrap strategy; fixes will follow dependency/import normalization. |
| R17 | needs_confirmation | `clearml/_entrypoint_bootstrap.py`, `scripts/_bootstrap.py`, `scripts/local_run.py`, `scripts/sync_clearml_templates.py`, `scripts/clearml_pipeline.py`, `pyproject.toml`, `uv.lock`, `deploy/base/Dockerfile`, `tests/test_clearml_mapping.py`, `tests/test_deploy_config.py` | `review/r02-dependency-import-runtime` | `scripts/_bootstrap.py` is a deletion candidate after uv workspace/package install is established. `clearml/_entrypoint_bootstrap.py` cannot be removed until direct script execution, `spec_from_file_location` tests, synced ClearML templates, and remote execution compatibility are verified. | `compileall` and `pytest` pass with current bootstrap. Docker installs editable core/tabular packages, but ClearML remote direct-entrypoint behavior remains manual verification required. | pending commit | Target repo may have different ClearML template paths and execution images; verify before deleting bootstrap compatibility code. | draft: local script bootstrap can likely be removed first, while ClearML entrypoint bootstrap remains gated by direct-entrypoint and remote verification. |

## Prompt 2-B Phase 2 implementation status override

The table below records the implementation state after the minimal Phase 2
dependency/import normalization. It supersedes the Prompt 2-A investigation
override for R02/R08/R16/R17.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R02 | done | `pyproject.toml`, `uv.lock`, `requirements.txt`, `requirements-dev.txt`, `.github/workflows/*.yml`, `docs/ml_platform_mkdocs/requirements-docs.txt` | `review/r02-dependency-import-runtime` | Added uv workspace metadata for `pkgs/core` and `pkgs/tabular`, root workspace sources, extras for `clearml` and `gbm`, dev/docs dependency groups, and committed `uv.lock`. CI, smoke, and MkDocs workflows now use `uv sync --frozen` / `uv run`. Requirements files remain as compatibility files for Docker/ClearML and legacy pip setup. `gitlint` remains in `requirements-dev.txt` because gitlint 0.19.x pins `sh==1.14.3`, which fails uv lock metadata build on Windows. | `uv lock` passed after excluding gitlint from uv dev group; `uv sync --all-extras --dev` passed; `uv run python -m compileall clearml pkgs scripts` passed; `uv run python -m pytest` passed 89 tests; `uv run lint-imports --config pyproject.toml` passed. | pending commit | Port `pyproject.toml` and `uv.lock` together. Re-check `uv lock` on the target OS and decide whether to keep gitlint as requirements-only or move it once a Windows-safe version is available. | draft: dependency management now has uv workspace and lockfile as the source of truth; requirements remain compatibility inputs for Docker/ClearML paths. |
| R08 | in_progress | `clearml/adapter.py`, `clearml/_entrypoint_bootstrap.py`, `clearml/app.py`, `clearml/pipelines.py`, `clearml/templates.py` | `review/r02-dependency-import-runtime` | Narrowed ClearML SDK import error handling and documented why `import_clearml_sdk()` remains while the operations directory is still named `clearml`. Official SDK import resolves to `.venv` under uv. Full local-directory rename is deferred to a compatibility-preserving runtime move. | PowerShell import probe confirmed official ClearML SDK path, core package path, and tabular package path. ClearML template sync dry-run and pipeline dry-run both passed. | pending commit | Keep this patch before any future `clearml/` rename. Target repo must confirm synced template entrypoints before removing the helper. | draft: official SDK imports are safer and better documented, while the `clearml/` runtime directory remains for template compatibility. |
| R16 | in_progress | `pyproject.toml`, `clearml/app.py`, `clearml/pipelines.py`, `clearml/templates.py`, `scripts/local_run.py`, `scripts/sync_clearml_templates.py`, `scripts/clearml_pipeline.py` | `review/r02-dependency-import-runtime` | Removed local script bootstrap and restored import order where package-installed imports now work. ClearML direct-entrypoint files still need bootstrap before sibling imports, so Ruff E402 is explicitly ignored only for those files with a comment tying the exception to the R18 runtime boundary migration. | `uv run python -m ruff check .` no longer reports E402. It still fails on out-of-scope F841 in `infer.py` and F401 in `pipeline.py`. `uv run python -m ruff format --check .` still reports 19 files would be reformatted. | pending commit | Keep E402 exception with direct-entrypoint compatibility. Do not mix broad formatting or tabular unused-symbol cleanup into this commit when porting. | draft: import-order linting is restored for local scripts, and remaining ClearML E402 exceptions are documented compatibility exceptions until runtime entrypoints move. |
| R17 | needs_confirmation | `clearml/_entrypoint_bootstrap.py`, `scripts/local_run.py`, `scripts/sync_clearml_templates.py`, `scripts/clearml_pipeline.py`, `uv.lock`, `pyproject.toml` | `review/r02-dependency-import-runtime` | Removed `scripts/_bootstrap.py` and its callers. Local scripts now rely on uv workspace/package-installed imports or ClearML entrypoint self-bootstrap. `clearml/_entrypoint_bootstrap.py` remains because remote templates still execute `clearml/app.py` and `clearml/pipelines.py` directly. | `uv run python scripts/local_run.py` training and inference smoke runs passed. `uv run python scripts/sync_clearml_templates.py --dry-run` and `uv run python scripts/clearml_pipeline.py --dry-run` passed. ClearML remote execution remains manual verification required. | pending commit | Verify target ClearML template entrypoints and remote Agent working directory before deleting `clearml/_entrypoint_bootstrap.py`. | draft: local script sys.path bootstrap has been removed; ClearML direct-entrypoint bootstrap remains intentionally until remote/template compatibility is verified. |

## Prompt 3-A Phase 3 investigation status override

The table below records the Phase 3-A investigation and classification for
adapter/config cleanup rows. It is intentionally investigation-only: no row is
marked `done` in this prompt.

Classification key: `quick_fix` means small local code/test changes should be
safe in Prompt 3-B; `needs_tests_first` means add/adjust characterization tests
before changing behavior; `needs_design` means Prompt 3-C or later should define
typed boundaries before broad edits; `blocked` means external/runtime behavior
cannot be proven locally.

| Review ID | Status | Classification | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|---|
| R05 | in_progress | needs_tests_first, needs_design | `clearml/adapter.py`, `clearml/templates.py`, `clearml/pipelines.py`, `clearml/reports.py`, `pkgs/core/src/ml_platform_core/result.py`, tabular estimator/report helpers | `review/r03-types-config-adapter` | `Any` is concentrated around ClearML SDK task/logger objects, artifact payloads, Plotly-like figure dicts, and the still-dict-based config surface. Prompt 3-B can introduce narrow Protocols for ClearML task/logger/artifact surfaces where tests already fake them; Prompt 3-C should handle config-wide `dict[str, Any]`. | `rg "\bAny\b"` shows broad use. No Protocol/TypedDict/StrEnum usage currently exists. `uv run python -m compileall clearml pkgs scripts` passed. | pending commit | Port adapter Protocols separately from config model work; target ClearML SDK version may require optional protocol methods. | draft: I confirmed the main `Any` pressure points and will first narrow ClearML boundary objects with characterization tests, leaving config-wide typing to the typed-config step. |
| R06 | in_progress | needs_tests_first | `clearml/adapter.py`, `clearml/templates.py`, `clearml/pipelines.py`, `clearml/reports.py`, `clearml/app.py`, tabular estimator helpers | `review/r03-types-config-adapter` | Predictable API `getattr` calls exist, but many are compatibility guards for ClearML SDK version differences or optional sklearn estimator attributes. Prompt 3-B should convert only stable fake-tested attributes and leave compatibility guards documented. | `rg "getattr\("` shows ClearML task/logger/status/name/type calls plus sklearn estimator attribute probes. Existing tests already fake ClearML task/adapter behavior in `tests/test_clearml_mapping.py`. | pending commit | Do not bulk-replace SDK compatibility `getattr` in target repo without confirming SDK version. | draft: I separated compatibility `getattr` usage from ordinary attribute access candidates and will adjust only the safe, tested subset first. |
| R07 | in_progress | needs_tests_first, needs_design | `clearml/adapter.py`, `clearml/app.py`, `clearml/pipelines.py`, `pkgs/tabular/src/ml_platform_tabular/stage.py`, tests | `review/r03-types-config-adapter` | Stage names are string literals in routing, tags, pipeline step construction, and tabular stage dispatch. A shared `StageName` Literal or small StrEnum should preserve current string values for artifact/tag compatibility. | `rg "stage: str|stage =="` shows `preprocess_features`, `train_model`, `build_ensemble`, and `evaluate_models` in adapter/app/pipelines/tabular stage dispatch. | pending commit | Keep serialized stage values unchanged when porting; only typing/helpers should change. | draft: stage values are now mapped; the next patch will add typed stage constants without changing the persisted ClearML/tag/artifact strings. |
| R09 | needs_confirmation | blocked, needs_tests_first | `clearml/adapter.py`, `clearml/app.py`, `clearml/templates.py`, source review references | `review/r03-types-config-adapter` | The reviewed `clearml_dataset_exists()` helper is not present in the current repo. The closest current behavior is `ClearMLAdapter.resolve_dataset()`, which imports `Dataset` and resolves dataset local copies inside the adapter after task init. Whether a separate entrypoint preflight is still required needs confirmation against intended ClearML runtime UX. | `rg "clearml_dataset_exists"` only matches review docs. ClearML localhost UI and remote execution remain manual verification required. | pending commit | Target repo may still have the reviewed helper; map this row to the actual dataset preflight implementation before porting. | draft: the exact reviewed helper is absent here; I identified the equivalent dataset resolution path and will only move checks upstream after confirming the intended runtime behavior. |
| R10 | in_progress | quick_fix, needs_tests_first | `clearml/adapter.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | `ClearMLAdapter.resolve_dataset()` accepts `dataset_id: str | None` because it also handles local-path fallback. After the `not dataset_id` branch, the ClearML `Dataset.get()` path can use a local `dataset_id_text: str` to make the narrowed type explicit. | Existing mapping tests cover local fallback and dataset path behavior indirectly; add a focused fake `Dataset.get()` test before the change. | pending commit | This should be portable as a tiny adapter-only patch. | draft: dataset id narrowing can be made explicit after the local fallback branch, with a small fake-Dataset test. |
| R11 | in_progress | quick_fix, needs_tests_first | `clearml/adapter.py`, `clearml/app.py`, `clearml/pipelines.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | `as_list()` always returns `list[str] | None`, so `as_str_list()` is the clearer name. Prompt 3-B can add `as_str_list`, update internal callers, and keep `as_list` as a temporary compatibility wrapper only if tests or external snippets still need it. | `rg "as_list"` shows runtime use in adapter/app/pipelines and existing mapping tests. | pending commit | Rename callers and wrapper in one commit; remove wrapper only if target repo has no external use. | draft: `as_list` is confirmed to mean string-list coercion, so I will rename it internally and preserve compatibility only where needed. |
| R12 | in_progress | quick_fix, needs_tests_first | `clearml/adapter.py`, `clearml/pipelines.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | `_ui_value()` serializes list/dict values for ClearML parameter transport. Prompt 3-B can rename it to `_to_clearml_parameter_value()` or equivalent and add a docstring without changing values. | Existing parameter tests assert JSON string values for metrics/features/ensemble methods. | pending commit | Keep serialized values identical across the rename to avoid ClearML template churn. | draft: `_ui_value` is a ClearML parameter transport normalizer; the next patch will rename/document it while preserving serialized output. |
| R15 | in_progress | needs_tests_first, needs_design | `clearml/adapter.py`, `clearml/pipelines.py`, `clearml/templates.py`, `clearml/app.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | `ui_params`, `default_ui_params`, `pipeline_ui_params`, and grouped UI helper names remain across ClearML runtime and tests. Prompt 3-B can introduce clearer internal aliases such as `default_runtime_params`, `connected_params`, and `pipeline_default_params`; full public rename should remain compatibility-aware. | `rg "ui_params|default_ui_params|pipeline_ui_params|_ui_value"` shows current runtime and test coverage. | pending commit | Port as compatibility-preserving rename; do not change ClearML parameter keys like `Input/*`, `Model/*`. | draft: the remaining UI vocabulary is parameter transport naming, not product UI keys; I will rename internals while keeping ClearML parameter keys stable. |
| R19 | in_progress | quick_fix, needs_tests_first | `pkgs/core/src/ml_platform_core/io.py`, tests | `review/r03-types-config-adapter` | `TABLE_SUFFIXES` is used for recursive discovery but not for direct file path or `preferred_name` validation. Prompt 3-B should add a helper such as `_is_supported_table_file()` and tests for direct unsupported files, preferred unsupported files, and recursive discovery. | `rg "TABLE_SUFFIXES"` confirms only `find_table_file()` recursive candidates use the suffix set today. | pending commit | File suffix policy should stay `.csv`, `.parquet`, `.pq` unless target repo intentionally differs. | draft: table suffix validation is missing on direct/preferred paths; I will add one helper and focused IO tests. |
| R20 | in_progress | quick_fix | `pkgs/core/src/ml_platform_core/config.py`, repository references | `review/r03-types-config-adapter` | `set_dotted_path = set_by_dotted_path` is not referenced by code or tests, only review docs. In this repo it is a deletion candidate after confirming no public import contract depends on it. | `rg "set_dotted_path"` finds only the alias and review docs. Core `__init__` does not export it. | pending commit | If the target repo has external imports, keep a deprecation wrapper there instead of deleting immediately. | draft: the alias is unused in this repo and not exported; I will remove it unless target-port evidence requires a deprecation period. |
| R26 | in_progress | needs_design | `pkgs/core/src/ml_platform_core/config.py`, `clearml/*`, `pkgs/tabular/src/ml_platform_tabular/*`, tests | `review/r03-types-config-adapter` | Config remains `dict[str, Any]` across core, ClearML runtime, and tabular domain code. Prompt 3-C should start at boundaries with small typed models or TypedDicts for run/data/model/features/output and pipeline plan objects; avoid a whole-repo rewrite. | `rg "dict[str, Any]|load_run_config|apply_overrides"` shows dict config is central. Existing smoke/mapping tests are the characterization base. | pending commit | Port typed config in small boundary commits; preserve YAML schema and public config keys. | draft: typed config needs a staged design, beginning at boundaries and preserving existing YAML/parameter schemas. |
| R27 | in_progress | quick_fix, needs_tests_first | `pkgs/core/src/ml_platform_core/registry.py`, `pkgs/core/src/ml_platform_core/__init__.py`, repository references | `review/r03-types-config-adapter` | `Registry` is not imported by package `__init__` and has no code/test references outside docs. In this repo it is a deletion candidate, with optional package import smoke tests to prove no public surface breaks. | `rg "Registry"` finds only docs and `registry.py`; `pkgs/core/src/ml_platform_core/__init__.py` exports only `RunResult`. | pending commit | If target repo has actual Registry users, do not delete; migrate those users or add tests first. | draft: `Registry` is unused and not exported here, so I will remove it with a smoke test unless target-port evidence shows live usage. |

## Prompt 3-B Phase 3 small-fix implementation status override

The table below records the small, test-backed Phase 3-B implementation. R26
remains intentionally out of scope for Prompt 3-C typed config work.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R05 | in_progress | `clearml/adapter.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | Added `ClearMLExecutionTask` Protocol and narrowed `apply_execution_image()` from `Any` to the protocol. Broader ClearML logger/artifact/config `Any` typing remains for later, especially R26. | Added fake task tests for normal and legacy `set_base_docker` forms. Targeted Ruff on `clearml`, `pkgs/core`, and changed tests passed. | pending commit | Port as a small adapter-boundary typing patch. Keep SDK compatibility Protocol methods flexible in target repo. | draft: task execution image handling is now typed through a narrow Protocol; wider config/payload `Any` cleanup remains staged. |
| R06 | in_progress | `clearml/adapter.py`, `clearml/app.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | Replaced the predictable `getattr(task, "set_base_docker", None)` path with direct API calls and version fallback. `app.py` now reads `adapter.task.id` directly after task init. SDK compatibility guards remain where ClearML versions differ. | Fake task tests cover direct API and legacy fallback. Full pytest passed. | pending commit | Do not bulk-replace ClearML SDK compatibility `getattr` in target repo without SDK version evidence. | draft: stable task APIs are direct calls now, while version-dependent ClearML compatibility guards remain intentionally. |
| R07 | in_progress | `pkgs/core/src/ml_platform_core/stages.py`, `clearml/adapter.py`, `clearml/pipelines.py`, `pkgs/tabular/src/ml_platform_tabular/stage.py`, tests | `review/r03-types-config-adapter` | Added shared `StageName` Literal and `as_stage_name()` validator. Adapter, pipeline step creation, and tabular stage dispatch now validate allowed stage names while preserving serialized strings. | Added unknown-stage validation test. Full pytest passed. | pending commit | Keep string values unchanged for ClearML tags, artifact references, and remote parameter compatibility. | draft: stage values now have a shared typed validator without changing persisted ClearML/runtime strings. |
| R09 | needs_confirmation | `clearml/adapter.py`, `clearml/app.py`, `clearml/templates.py`, `clearml/pipelines.py` | `review/r03-types-config-adapter` | Added `validate_clearml_runtime()` and call it near ClearML entrypoints before runtime work. Added narrow `clearml_dataset_exists(dataset_id: str)` for dataset existence only. Full ClearML localhost/remote UX remains manual verification. | Fake Dataset test covers existence helper. Local ClearML remote/UI not executed. | pending commit | Target repo may still have a different reviewed helper; map this change to the actual entrypoint preflight before porting. | draft: ClearML runtime import validation moved upward, and dataset existence is now a narrow helper; remote/UI behavior still needs manual confirmation. |
| R10 | done | `clearml/adapter.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | Added `clearml_dataset_exists(dataset_id: str)` and narrowed `resolve_dataset()` to a stripped `dataset_id_text` before calling `Dataset.get()`. Local-path fallback still accepts `None`. | Fake Dataset test covers string normalization, missing dataset, and empty ID rejection. Full pytest passed. | pending commit | Portable adapter-only change; keep fallback semantics if target repo depends on local path mode. | draft: dataset id handling is explicit after fallback, and the standalone existence check accepts only a non-empty string. |
| R11 | done | `clearml/adapter.py`, `clearml/app.py`, `clearml/pipelines.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | Introduced `as_str_list()` and moved internal ClearML runtime callers to it. `as_list()` remains only as a deprecated compatibility wrapper. | Added conversion/compatibility assertions. Full pytest passed. | pending commit | Keep wrapper during target-port unless external callers are proven absent. | draft: `as_list` has been clarified as `as_str_list` internally, with a compatibility wrapper for existing callers. |
| R12 | done | `clearml/adapter.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | Renamed `_ui_value()` behavior to `_to_clearml_parameter_value()` with a transport-focused docstring and unchanged serialization. | Existing mapping tests plus runtime/default compatibility test confirm serialized values remain stable. | pending commit | No ClearML parameter value churn expected; port together with R15 naming wrappers. | draft: the helper now describes ClearML parameter transport rather than UI value semantics, while output values remain identical. |
| R15 | in_progress | `clearml/adapter.py`, `clearml/app.py`, `clearml/templates.py`, `clearml/pipelines.py`, `tests/test_clearml_mapping.py` | `review/r03-types-config-adapter` | Added and used `default_runtime_params`, `grouped_runtime_params`, `apply_runtime_params`, `pipeline_runtime_params`, and `_task_runtime_params`. Deprecated `ui_*` wrappers remain for compatibility, so full public vocabulary cleanup is still staged. | Compatibility tests compare old/new helper outputs. Targeted Ruff on changed runtime files passed. | pending commit | Keep ClearML parameter keys (`Input/*`, `Model/*`, etc.) stable. Remove wrappers only after target consumers are audited. | draft: internal naming now uses runtime/connected parameter vocabulary, with UI-named wrappers retained for compatibility. |
| R19 | done | `pkgs/core/src/ml_platform_core/io.py`, `tests/test_core_smoke.py` | `review/r03-types-config-adapter` | Added `is_supported_table_file()` and used it for direct file, preferred file, and recursive directory discovery paths. Unsupported direct/preferred files now fail early. | Added direct unsupported, preferred unsupported, and directory-search suffix tests. Full pytest passed. | pending commit | Keep supported suffix set identical unless target repo intentionally differs. | draft: table suffix validation is now shared across direct, preferred, and directory discovery paths. |
| R20 | done | `pkgs/core/src/ml_platform_core/config.py`, `tests/test_core_smoke.py` | `review/r03-types-config-adapter` | Removed unused `set_dotted_path` alias after confirming no code/test references and no package export. | Added smoke assertion that config no longer exposes the alias. Full pytest passed. | pending commit | If target repo has external imports, keep a deprecation wrapper there instead of deleting immediately. | draft: unused non-exported alias was removed after repository-wide reference checks. |
| R27 | done | `pkgs/core/src/ml_platform_core/registry.py`, `tests/test_core_smoke.py` | `review/r03-types-config-adapter` | Removed unused `Registry` module because it had no implementation/test references and was not exported. | Added smoke assertion that `ml_platform_core.registry` is not importable and file is absent. Full pytest passed. | pending commit | Do not delete in target repo if actual users exist; migrate/test those users first. | draft: the unused Registry abstraction was removed rather than preserved without a concrete use. |

## Prompt 3-C R26 typed config boundary status override

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R26 | in_progress | `pkgs/core/src/ml_platform_core/config_models.py`, `pkgs/core/src/ml_platform_core/config.py`, `tests/test_config_models.py`, config consumers | `review/r03-types-config-adapter` | Added a dataclass-based typed config boundary without adding Pydantic. `parse_run_config()` validates known sections, preserves unknown keys as extras, and exposes `RunConfig.to_dict()` for compatibility. `load_typed_run_config()` is the typed entrypoint. Existing `load_run_config()` still returns a dict, but now validates through the typed parser so invalid known config fails early. Downstream ClearML/tabular consumers still use dicts and should migrate incrementally. | Added tests for valid minimal parse, defaults, wrong-type errors, unknown-key preservation, override parsing through `load_typed_run_config()`, and `load_run_config()` dict compatibility. `uv run python -m compileall clearml pkgs scripts` passed. `uv run python -m pytest` passed 102 tests. Full Ruff still fails on pre-existing F841/F401 and format debt. | pending commit | Port `config_models.py` and the `config.py` validation wrapper together. Keep YAML schema and dict-returning `load_run_config()` compatibility until target consumers migrate. | draft: R26 now has a typed external config boundary and early validation while preserving existing dict callers; full downstream typed access can proceed in small follow-up steps. |

## Prompt 4-A R18 manifest scaffold status override

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R18 | in_progress | `pkgs/core/src/ml_platform_core/contracts.py`, `pkgs/core/src/ml_platform_core/runtime_types.py`, `pkgs/tabular/src/ml_platform_tabular/manifest.py`, `pkgs/tabular/src/ml_platform_tabular/policy.py`, `clearml/pipelines.py`, `tests/test_runtime_manifest.py` | `review/r04-runtime-manifest-boundary` | Added ClearML-free core runtime contracts, runtime adapter protocols, tabular policy constants, tabular package manifest, and a runtime-neutral tabular domain pipeline plan builder. Existing ClearML runtime behavior is intentionally unchanged in this prompt. `clearml/pipelines.py` still owns tabular graph assembly, parameter defaults, model suite application, and quality mode application until Prompt 4-B connects the runtime to the manifest. | Added manifest tests for load/unique keys, runner path resolution without ClearML, required parameters/artifacts, duplicate key validation, contract kind validation, policy preset copy safety, and ClearML-free `DomainPipelinePlan` construction. `uv run python -m compileall clearml pkgs scripts` passed. `uv run python -m pytest` passed 110 tests. Full Ruff still fails on pre-existing F841/F401 and broad format debt; targeted Ruff/format on new files passed. | pending commit | Port the core contracts and tabular manifest/policy together before any runtime renderer changes. Keep direct `clearml/app.py` and `clearml/pipelines.py` template compatibility until the target repo verifies ClearML remote entrypoints. | draft: R18 now has the first core contract and tabular manifest scaffold, with tests. The ClearML runtime still contains tabular policy temporarily and will consume the manifest in the next incremental prompt. |

## Prompt 4-B R18 manifest renderer status override

The table below records the incremental connection from the tabular manifest /
domain plan to the ClearML runtime renderer. It supersedes the Prompt 4-A note
that `clearml/pipelines.py` still owned graph assembly and basic preset policy.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| R18 | in_progress | `clearml/pipelines.py`, `pkgs/core/src/ml_platform_core/contracts.py`, `pkgs/tabular/src/ml_platform_tabular/manifest.py`, `pkgs/tabular/src/ml_platform_tabular/policy.py`, `tests/test_runtime_manifest.py`, `tests/test_clearml_mapping.py` | `review/r04-runtime-manifest-boundary` | Moved tabular model-suite selection, quality-mode preset application, runtime parameter defaults, candidate normalization, ensemble policy, and training graph construction into `ml_platform_tabular.policy` / `ml_platform_tabular.manifest`. `clearml/pipelines.py` now builds a ClearML-free `DomainPipelinePlan` and renders it into existing ClearML `PipelineController` step definitions. ClearML SDK operations, project/queue/tag wiring, task draft lifecycle, script metadata, and direct template entrypoint compatibility remain in the runtime layer. | `python -m ...` commands fail because PATH `python` is the Windows Store alias. `uv run python -m compileall clearml pkgs scripts` passed. `uv run python -m pytest` passed 112 tests. Targeted R18 tests and targeted Ruff/format on changed files passed. Full Ruff still fails on pre-existing F841 in `infer.py`, F401 in `pipeline.py`, and broad format debt in 18 files. ClearML localhost UI, remote execution, and Kubernetes checks are manual verification required. | pending commit | Port this after the Phase 4-A contract/manifest scaffold. Keep stage names, parameter keys, and direct `clearml/app.py` / `clearml/pipelines.py` entrypoints stable. Re-run ClearML template dry-run and remote/UI checks in the target repo before marking R18 fully done. | draft: R18 now has a manifest/provider boundary in practice: tabular owns presets and domain plan construction, while ClearML runtime renders that plan into PipelineController calls. Server-backed ClearML verification remains pending. |

## Map

| Review ID | Priority | Source document | Summary | Current repo suspected files | Status | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R01 | imo | source/pr28_review_consolidated.md | ruff / ty / radon / import-linter 等の静的解析を復旧 | pyproject.toml, requirements-dev.txt, .pre-commit-config.yaml, .github/workflows/ci.yml | needs_confirmation | review/r01-tooling-ci | Prompt 1-A調査では元の静的解析設定は履歴に見つからない。Prompt 1-BでRuff/format/basic hooks中心の最小構成案を作る。ty/import-linter/radon採用範囲は要確認。 | `.venv`ではpytestのみ利用可。ruff/pre_commit/gitlint/radon/lint_importsは未導入。`python`はWindows Store aliasで失敗。 | TBD | 移植先でもdev tool定義、runner、required check差分を確認し、Commit/Porting noteを更新。 | R01調査済み: 元設定は現repo履歴に見つからないため、最小構成で復旧案を作成予定。 |
| R02 | imo | source/pr28_review_consolidated.md | requirements中心から uv / pyproject / lock 管理へ移行 | pyproject.toml, uv.lock, requirements*.txt, pkgs/core/pyproject.toml, pkgs/tabular/pyproject.toml | needs_confirmation | review/r02-dependency-import-runtime | Phase 2でuv workspace/lock方針を決めて対応。`uv sync --dry-run`はlock作成と環境変更を示したため、このPhaseでは同期しない。 | Phase 0: uv 0.11.16あり、uv.lockなし、uv checkはoutdated。Phase 2でlock/requirements整合を検証。 | TBD | lockfile有無とPython version差分が移植先でconflictしやすいため単独適用。 | R02対応予定: requirements中心から uv / pyproject / lock 管理へ移行。対応commitと検証結果は完了後に追記します。 |
| R03 | q | source/pr28_review_consolidated.md | BLAS/OpenMP thread env を共通Pythonコードではなく実行環境側へ移動 | clearml/app.py, scripts/local_run.py, scripts/make_sample_data.py, .github/workflows/*, deploy/* | deferred | review/r07-clearml-k8s-evidence | Phase 7で実行環境側へ移す。現状は3 Python entrypointにenv defaultがある。 | Phase 0: 対象箇所検索のみ。ClearML/Kubernetesはmanual verification required。 | TBD | target環境のrunner/cluster設定に依存するため、移植先で再検証。 | R03対応予定: BLAS/OpenMP thread env を共通Pythonコードではなく実行環境側へ移動。対応commitと検証結果は完了後に追記します。 |
| R04 | untagged | source/pr28_review_consolidated.md | 対象クラスタでの構成変更検証証跡を残す | deploy/**, docs/review/*, verification/** | blocked | review/r07-clearml-k8s-evidence | Phase 7で対象クラスタ証跡を追加。現環境ではKubernetes実機検証不可。 | Phase 0: manual verification requiredとして記録。実機証跡待ち。 | TBD | target repo/clusterで実施した証跡をPorting noteへ追記。 | R04対応予定: 対象クラスタでの構成変更検証証跡を残す。対応commitと検証結果は完了後に追記します。 |
| R05 | must | source/pr28_review_consolidated.md | 明確な入力型の Any を Task または Protocol へ狭める | clearml/adapter.py, tests/test_clearml_mapping.py | todo | review/r03-types-config-adapter | Phase 3でClearML Task Protocol等を導入し、明確な境界から小さく型を狭める。 | Phase 0: Any/getattr検索済み。Phase 3でtype/lint/pytest再検証。 | TBD | 型導入はClearML SDK version差に注意して小さいcommitで移植。 | R05対応予定: 明確な入力型の Any を Task または Protocol へ狭める。対応commitと検証結果は完了後に追記します。 |
| R06 | must | source/pr28_review_consolidated.md | 予測可能APIへの getattr を通常属性アクセスへ変更 | clearml/adapter.py, clearml/templates.py, clearml/pipelines.py | todo | review/r03-types-config-adapter | Phase 3で互換性が必要な箇所と通常属性アクセスへ戻せる箇所を分離する。 | Phase 0: getattr検索済み。Phase 3でClearML fake/pytest/lintを追加。 | TBD | SDK互換目的のgetattrは理由を残し、それ以外を小さく移植。 | R06対応予定: 予測可能APIへの getattr を通常属性アクセスへ変更。対応commitと検証結果は完了後に追記します。 |
| R07 | imo | source/pr28_review_consolidated.md | stage自由文字列を Literal / StrEnum などで型付け | clearml/adapter.py, pkgs/core, pkgs/tabular/stage.py, tests | todo | review/r03-types-config-adapter | Phase 3でStageKind等を導入し、stage名の自由文字列を段階的に型付けする。 | Phase 0: 対象候補のみ確認。Phase 3で既存stage出力互換テストを確認。 | TBD | 型名・値は既存artifact/stage名と互換にして移植。 | R07対応予定: stage自由文字列を Literal / StrEnum などで型付け。対応commitと検証結果は完了後に追記します。 |
| R08 | q | source/pr28_review_consolidated.md | ClearML SDK dynamic import / local clearml shadow 問題を整理 | clearml/adapter.py, clearml/_entrypoint_bootstrap.py, pyproject.toml, scripts/_bootstrap.py | todo | review/r02-dependency-import-runtime | Phase 2でpackage install/entrypoint方針と合わせてshadow回避を整理する。 | Phase 0: import_clearml_sdk/_entrypoint_bootstrap/sys.path検索済み。Phase 2でlocal/remote entrypoint検証。 | TBD | 移植先のrepo名/パッケージ名差分に注意。 | R08対応予定: ClearML SDK dynamic import / local clearml shadow 問題を整理。対応commitと検証結果は完了後に追記します。 |
| R09 | must | source/pr28_review_consolidated.md | ClearML設定確認を下位関数からentrypoint近傍へ移動 | clearml/adapter.py, clearml/app.py, clearml/templates.py | todo | review/r03-types-config-adapter | Phase 3で設定確認と下位処理を分離し、entrypoint近傍で失敗させる。 | Phase 0: 対象ファイル確認のみ。ClearML localhost UIはmanual verification required。 | TBD | 移植先のClearML設定方式に合わせて確認メッセージを調整。 | R09対応予定: ClearML設定確認を下位関数からentrypoint近傍へ移動。対応commitと検証結果は完了後に追記します。 |
| R10 | imo | source/pr28_review_consolidated.md | None除外済み dataset_id は str に狭める | clearml/adapter.py, tests/test_clearml_mapping.py | todo | review/r03-types-config-adapter | Phase 3でNone除外後の型を狭め、dataset取得前後の境界を明確化する。 | Phase 0: 対象ファイル確認のみ。Phase 3でunit test追加。 | TBD | ClearML Dataset API差分に注意。 | R10対応予定: None除外済み dataset_id は str に狭める。対応commitと検証結果は完了後に追記します。 |
| R11 | imo | source/pr28_review_consolidated.md | as_list を as_str_list 等へ改名 | clearml/adapter.py, clearml/pipelines.py, tests/test_clearml_mapping.py | todo | review/r03-types-config-adapter | Phase 3で用途に合わせて命名し、互換wrapperが必要なら一時的に残す。 | Phase 0: as_list周辺は未変更。Phase 3でmapping tests更新。 | TBD | renameは呼び出し側と同じcommitで移植。 | R11対応予定: as_list を as_str_list 等へ改名。対応commitと検証結果は完了後に追記します。 |
| R12 | nr | source/pr28_review_consolidated.md | _ui_value の用途を名称/docstringで明確化 | clearml/adapter.py, clearml/pipelines.py, tests/test_clearml_mapping.py | todo | review/r03-types-config-adapter | Phase 3でruntime parameter transport語彙へ寄せる。互換上`ui_*`を残す場合は理由を明記する。 | Phase 0: ui_*検索済み。Phase 3でmapping互換テスト。 | TBD | 互換wrapper名が残る場合はPorting noteに理由を記録。 | R12対応予定: _ui_value の用途を名称/docstringで明確化。対応commitと検証結果は完了後に追記します。 |
| R13 | must | source/pr28_review_consolidated.md | GitHub Actions runner set を正しいものへ戻す | .github/workflows/ci.yml | needs_confirmation | review/r01-tooling-ci | 現`ci.yml`は`ubuntu-latest`。レビュー元runner set `arc-runner-set-spdml-ml-pipeline` がこのrepoで使えるかはローカル確認不可。Prompt 1-BではTODOとして残す。 | 履歴候補は`ci.yml`のみ。runner availabilityはGitHub/target repo設定確認待ち。 | TBD | 移植先でrunner labelを必ず確認し、違う場合は`needs_confirmation`のまま適用しない。 | R13調査済み: runner set名は確認対象として記録し、利用可否は移植先/Repository設定確認待ち。 |
| R14 | must | source/pr28_review_consolidated.md | 共通CIを復旧し smoke workflow を分離 | .github/workflows/ci.yml, .github/workflows/smoke-test.yml, config/tasks/*.yaml | in_progress | review/r01-tooling-ci | 現`ci.yml`はsmoke主体で、存在しない`tabular_train.yaml`/`tabular_eval.yaml`/`tabular_1d_output.yaml`を呼ぶ。Prompt 1-Bで共通CIと現行task smokeを分離する。 | 履歴候補`647bcdf`/`6637119`確認済み。現在存在するtaskは`tabular_pipeline.yaml`/`tabular_stage.yaml`/`tabular_infer.yaml`。 | TBD | target repoのbranch protection/required checksに合わせ、common CIとsmoke workflowを別commitまたは明確な差分で移植。 | R14調査済み: `ci.yml`は旧task参照を含むため、共通CI復旧とsmoke workflow分離をPrompt 1-Bで実施予定。 |
| R15 | imo | source/pr28_review_consolidated.md | UI語彙を runtime_params / connected_params / default_params へ整理 | clearml/adapter.py, clearml/pipelines.py, clearml/templates.py, clearml/app.py, tests/test_clearml_mapping.py | todo | review/r03-types-config-adapter | Phase 3で語彙整理。ClearML画面互換のpublic surfaceを壊さず段階的にrenameする。 | Phase 0: ui_* match count記録済み。Phase 3でmapping testsを維持。 | TBD | rename conflictが出やすいため、compat wrapperとテストを同時移植。 | R15対応予定: UI語彙を runtime_params / connected_params / default_params へ整理。対応commitと検証結果は完了後に追記します。 |
| R16 | must | source/pr28_review_consolidated.md | import をファイル先頭へ戻し Ruff 等で検査 | clearml/templates.py, clearml/pipelines.py, clearml/_entrypoint_bootstrap.py | todo | review/r02-dependency-import-runtime | Phase 2でbootstrap/shadow整理後にimport位置を戻す。 | Phase 0: ruff未導入で検査不可。Phase 2でruff復旧後に確認。 | TBD | import orderはR01 tooling復旧後のルールに合わせる。 | R16対応予定: import をファイル先頭へ戻し Ruff 等で検査。対応commitと検証結果は完了後に追記します。 |
| R17 | imo | source/pr28_review_consolidated.md | 手動 sys.path 操作を package install / uv workspace で解消 | clearml/_entrypoint_bootstrap.py, scripts/_bootstrap.py, pyproject.toml, uv.lock | todo | review/r02-dependency-import-runtime | Phase 2でconsole/module entrypoint化とuv workspace方針を決める。 | Phase 0: sys.path bootstrap検索済み。Phase 2でlocal script/ClearML dry-runを確認。 | TBD | 移植先の実行方法とremote template互換に注意。 | R17対応予定: 手動 sys.path 操作を package install / uv workspace で解消。対応commitと検証結果は完了後に追記します。 |
| R18 | imo | source/repository_review_transcription_current.md + source/pr28_review_consolidated.md | 中央runtimeへ全packageが合流する構造を避けmanifest/provider境界へ | clearml/pipelines.py, clearml/adapter.py, clearml/templates.py, clearml/app.py, pkgs/core, pkgs/tabular/manifest.py, docs/adr | deferred | review/r04-runtime-manifest-boundary | Phase 0でADR作成済み。実装はPhase 4でmanifest/contractsから段階的に開始し、全面rewriteしない。 | Phase 0: ADR追加のみ。Phase 4でcontract/manifest testsを追加。 | TBD | ADRと初期manifestを先に移植し、その後runtime変更を分割適用。 | R18対応予定: 中央runtimeへ全packageが合流する構造を避けmanifest/provider境界へ。対応commitと検証結果は完了後に追記します。 |
| R19 | nits | source/pr28_review_consolidated.md | TABLE_SUFFIXES を直接指定・探索・glob の全経路で適用 | pkgs/core/src/ml_platform_core/io.py, tests | todo | review/r03-types-config-adapter | Phase 3で直接指定/探索/globの全経路を確認し、抜けがあれば小さく修正する。 | Phase 0: TABLE_SUFFIXES位置確認済み。Phase 3でio tests追加/更新。 | TBD | file suffix policy差分があればPorting noteへ記録。 | R19対応予定: TABLE_SUFFIXES を直接指定・探索・glob の全経路で適用。対応commitと検証結果は完了後に追記します。 |
| R20 | imo | source/pr28_review_consolidated.md | 未使用の後方互換aliasを確認後削除 | pkgs/core/src/ml_platform_core/config.py, repository-wide references | needs_confirmation | review/r03-types-config-adapter | Phase 3で`set_dotted_path`使用有無と互換要求を確認してから削除/維持を判断する。 | Phase 0: alias存在確認済み。Phase 3でrgとtestsで互換確認。 | TBD | 移植先に外部参照がある場合は削除せずdeprecationにする。 | R20対応予定: 未使用の後方互換aliasを確認後削除。対応commitと検証結果は完了後に追記します。 |
| R21 | untagged | source/pr28_review_consolidated.md | .gitlint と .pre-commit-config.yaml を復旧 | .gitlint, .pre-commit-config.yaml, requirements-dev.txt, pyproject.toml | needs_confirmation | review/r01-tooling-ci | 履歴に元ファイルなし。Prompt 1-BではRuff check/format、basic file hygiene、gitlintの最小構成案を作る。uv移行はR02へ回す。 | `git log --all --name-status`で候補なし。`.venv`にpre_commit/gitlintなし。 | TBD | target repoに既存hookがある場合は上書きせず差分統合。agent commit規約との整合を確認。 | R21調査済み: 元設定は見つからないため、最小pre-commit/gitlint構成で復旧案を作成予定。 |
| R22 | untagged | source/pr28_review_consolidated.md | .gitattributes を復旧 | .gitattributes | needs_confirmation | review/r01-tooling-ci | 履歴に元ファイルなし。Prompt 1-Bではtext auto、Python/YAML/Markdown LF、Windows script CRLF、binary保護の最小案を作る。 | `git log --all --name-status -- .gitattributes`で候補なし。 | TBD | 改行正規化で大きな差分が出る場合は別commit化し、移植先で確認。 | R22調査済み: 元設定は見つからないため、OS差分を減らす最小`.gitattributes`案を作成予定。 |
| R23 | untagged | source/pr28_review_consolidated.md | MkDocs deploy workflow を復旧 | .github/workflows/deploy-mkdocs.yml, docs/ml_platform_mkdocs/mkdocs.yml, docs/ml_platform_mkdocs/requirements-docs.txt | needs_confirmation | review/r01-tooling-ci | 履歴にdeploy workflowなし。deploy先/permissions/secretsは未確認。Prompt 1-Bでは少なくともdocs build check、deployは確認待ちまたはbuild-only案にする。 | `docs/ml_platform_mkdocs/mkdocs.yml`と`requirements-docs.txt`は存在。deploy workflow候補なし。 | TBD | GitHub Pages/environment/secretsは移植先で確認。未確認のdeployは有効化しない。 | R23調査済み: deploy workflowの元内容は見つからないため、deploy先確認待ちでbuild/check中心の復旧案を作成予定。 |
| R24 | untagged | source/pr28_review_consolidated.md | VS Code共有設定を復旧 | .vscode/extensions.json, .vscode/settings.json | needs_confirmation | review/r01-tooling-ci | 履歴に`.vscode/`なし。Prompt 1-Bでは個人設定を含めず、Ruff/Python/pytest/MkDocsに寄せた最小共有設定案を作る。 | `find .vscode`相当で欠落確認。履歴候補なし。 | TBD | target repoのチーム設定があれば優先。個人pathやsecretは混ぜない。 | R24調査済み: 元設定は見つからないため、個人依存のない最小VS Code共有設定案を作成予定。 |
| R25 | untagged | source/pr28_review_consolidated.md | package開発用 code-workspace を復旧 | ml_platform.code-workspace, *.code-workspace | needs_confirmation | review/r01-tooling-ci | 履歴にworkspaceなし。Prompt 1-Bではrepo相対pathのみの`ml_platform.code-workspace`最小案を作る。 | `git log --all --name-status -- '*.code-workspace'`で候補なし。 | TBD | 移植先のrepo名/path構成に合わせる。絶対pathは禁止。 | R25調査済み: 元workspaceは見つからないため、現`pkgs/`構成に合わせた最小workspace案を作成予定。 |
| R26 | must | source/pr28_review_consolidated.md | 巨大 dict[str, Any] config を dataclass / Pydantic 等の型付きモデルへ | pkgs/core/src/ml_platform_core/config.py, clearml/*, pkgs/tabular/*, tests | todo | review/r03-types-config-adapter | Phase 3で境界から小さく型付きモデルを導入。大規模rewriteは避ける。 | Phase 0: Any検索済み。Phase 3でcharacterization/pytest/lintを追加。 | TBD | 型モデル導入はpublic config互換を保って段階移植。 | R26対応予定: 巨大 dict[str, Any] config を dataclass / Pydantic 等の型付きモデルへ。対応commitと検証結果は完了後に追記します。 |
| R27 | must | source/pr28_review_consolidated.md | 未使用 Registry を削除、または実利用とテストを追加 | pkgs/core/src/ml_platform_core/registry.py, repository-wide references, tests | needs_confirmation | review/r03-types-config-adapter | Phase 3で未使用なら削除、将来利用するなら実利用とテストを追加。どちらにするか確認が必要。 | Phase 0: Registry存在確認済み。Phase 3でrgとtestsで判断。 | TBD | target repoで参照がある場合は削除せず方針を合わせる。 | R27対応予定: 未使用 Registry を削除、または実利用とテストを追加。対応commitと検証結果は完了後に追記します。 |
## Prompt 5 TABULAR-SPLIT characterization status override

This entry tracks the additional tabular package review item that is outside
R01-R27. It is related to R18 and R26 because it freezes tabular output
contracts before the Phase 6 module split.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| TABULAR-SPLIT | done | `tests/test_tabular_characterization.py`, `pkgs/tabular/src/ml_platform_tabular/pipeline.py`, `pkgs/tabular/src/ml_platform_tabular/infer.py`, `pkgs/tabular/src/ml_platform_tabular/plots.py` | `review/r05-tabular-characterization-tests` | Added characterization tests before splitting tabular modules. The tests pin training artifact/table/plot/metric keys, leaderboard and decision summary shape, inference slim prediction columns, inference manifest/schema summary fields, and standalone plot writer behavior on tiny seeded data. No production module split was performed in this phase. | `uv run python -m pytest tests/test_tabular_characterization.py` passed: 3 passed. `uv run python -m pytest` passed: 115 passed. `uv run python -m compileall clearml pkgs scripts` passed. Targeted Ruff for the new test passed. Prompt-style `python -m ...` commands fail because PATH `python` is the Windows Store alias. Full Ruff still reports pre-existing F841 in `infer.py`, F401 in `pipeline.py`, and broad format debt in existing files. | pending commit | Port this test commit before splitting `infer.py`, `pipeline.py`, or `plots.py`. Treat expected key lists and dataframe column order as compatibility contracts; update them only with intentional migration notes. | draft: Added ClearML-free characterization coverage for tabular training, inference, and plotting outputs so the upcoming module split can preserve artifact names, metrics, dataframe schemas, and summary contracts. |
## Prompt 6 TABULAR-SPLIT plots split status override

This entry records the first Phase 6 module split. The overall
TABULAR-SPLIT item remains `in_progress` because `infer.py` and `pipeline.py`
are still pending.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| TABULAR-SPLIT | in_progress | `pkgs/tabular/src/ml_platform_tabular/plots.py`, `pkgs/tabular/src/ml_platform_tabular/plotting/*`, `pkgs/tabular/src/ml_platform_tabular/pipeline.py`, `pkgs/tabular/src/ml_platform_tabular/infer.py`, `pkgs/tabular/src/ml_platform_tabular/metrics.py`, `tests/test_tabular_plots.py`, `tests/test_tabular_characterization.py` | `review/r06-tabular-module-split` | Split plotting responsibilities into the new `plotting` package: `common`, `feature`, `prediction`, `candidate`, `leaderboard`, and `summary`. Kept `ml_platform_tabular.plots` as a compatibility facade that re-exports the existing public API. Internal tabular callers now import from `ml_platform_tabular.plotting`. Removed two tiny lint leftovers in touched tabular files: unused `numpy` in `pipeline.py` and unused `data_cfg` in `infer.py`. | `uv run python -m pytest tests/test_tabular_characterization.py tests/test_tabular_plots.py` passed: 5 passed. `uv run python -m compileall clearml pkgs scripts` passed. `uv run python -m pytest` passed: 116 passed. `uv run python -m ruff check .` passed. Prompt-style `python -m ...` commands still fail because PATH `python` is the Windows Store alias. `uv run python -m ruff format --check .` still reports existing broad format debt in 16 files. | pending commit | Port this after the characterization test commit and before splitting inference. Keep the `plots.py` facade until target code no longer imports it directly. Do not create a same-name `plots/` package while `plots.py` remains. | draft: Plotting responsibilities are now split by domain while the existing `ml_platform_tabular.plots` import path remains compatible; characterization tests and full pytest still pass. |

## Prompt 6 TABULAR-SPLIT inference split status override

This entry records the second Phase 6 module split. The overall
TABULAR-SPLIT item remains `in_progress` because `pipeline.py` is still pending.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| TABULAR-SPLIT | in_progress | `pkgs/tabular/src/ml_platform_tabular/infer.py`, `pkgs/tabular/src/ml_platform_tabular/inference/*`, `tests/test_infer_schema_check.py`, `tests/test_tabular_characterization.py`, `tests/test_tabular_smoke.py`, `tests/test_pipeline_smoke.py` | `review/r06-tabular-module-split` | Split inference responsibilities into the new `inference` package: `resolver`, `metadata`, `schema`, `prediction_frame`, `prediction_writer`, and `runner`. Kept `ml_platform_tabular.infer` as a compatibility facade that preserves `run_infer` and currently used private helper imports such as `_prediction_frame` and `_schema_check_summary`. Existing runner paths in manifest/runners remain unchanged. | `uv run python -m pytest tests/test_infer_schema_check.py tests/test_tabular_characterization.py tests/test_tabular_smoke.py tests/test_pipeline_smoke.py` passed: 22 passed. `uv run python -m compileall clearml pkgs scripts` passed. `uv run python -m pytest` passed: 116 passed. `uv run python -m ruff check .` passed. Prompt-style `python -m ...` commands still fail because PATH `python` is the Windows Store alias. `uv run python -m ruff format --check .` still reports existing broad format debt in 16 files. | pending commit | Port this after the characterization and plotting split commits. Keep the `infer.py` facade until target consumers and ClearML runner paths are migrated intentionally. Do not change output filenames, prediction column order, or schema summary semantics without updating characterization tests. | draft: Inference responsibilities are now split by resolver/metadata/schema/prediction writer/runner boundaries while `ml_platform_tabular.infer:run_infer` remains compatible; characterization and full pytest still pass. |

## Prompt 6 TABULAR-SPLIT pipeline split status override

This entry records the third Phase 6 module split. The main `plots.py`,
`infer.py`, and `pipeline.py` splits are implemented; compatibility facades are
kept for public runner paths and existing tests.

| Review ID | Status | Current repo suspected files | Planned branch | Implementation note | Tests / checks | Commit | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|---|---|
| TABULAR-SPLIT | done | `pkgs/tabular/src/ml_platform_tabular/pipeline.py`, `pkgs/tabular/src/ml_platform_tabular/training/*`, `pkgs/tabular/src/ml_platform_tabular/stage.py`, `tests/test_decision_summary.py`, `tests/test_tabular_characterization.py`, `tests/test_pipeline_smoke.py`, `tests/test_stage_smoke.py` | `review/r06-tabular-module-split` | Split pipeline responsibilities into the new `training` package: `preprocessing`, `candidate_training`, `ensemble`, `evaluation`, `ranking`, `summary`, `recommendation`, `artifacts`, and `orchestrator`. Added `EvaluationResult` dataclass as the visible evaluate-models artifact boundary while `_evaluate_models()` remains a dict-returning compatibility wrapper. Kept `ml_platform_tabular.pipeline` as a compatibility facade for `run_pipeline` and stage/private helper imports used by existing code. | `uv run python -m pytest tests/test_decision_summary.py tests/test_tabular_characterization.py tests/test_pipeline_smoke.py tests/test_tabular_smoke.py tests/test_stage_smoke.py` passed: 22 passed. `uv run python -m compileall clearml pkgs scripts` passed. `uv run python -m pytest` passed: 117 passed. `uv run python -m ruff check .` passed. Prompt-style `python -m ...` commands still fail because PATH `python` is the Windows Store alias. `uv run python -m ruff format --check .` still reports existing broad format debt in 16 files. | pending commit | Port after characterization, plotting, and inference split changes. Keep `pipeline.py` facade until ClearML runner paths, `stage.py`, and external callers are intentionally migrated to `training.*`. Do not change artifact/table/plot names without updating characterization tests. | draft: Pipeline responsibilities are now split by training domain while `ml_platform_tabular.pipeline:run_pipeline` and stage helper imports remain compatible; evaluate-models outputs have an explicit `EvaluationResult` boundary and characterization/full pytest pass. |

## Final validation status override - 2026-06-29, no K8 scope

This section is the final status source for the integration branch
`review/pr28-complete-response`. Kubernetes / K8 verification is intentionally
excluded from this repository cleanup. The missing
`review/r07-clearml-k8s-evidence` branch was not merged.

Validation summary:

- Prompt-style `python -m ...` commands fail on this machine because PATH
  `python` is the Windows Store execution alias.
- `uv run python -m compileall clearml pkgs scripts` passed.
- `uv run python -m pytest` passed: 117 tests.
- `uv run python -m ruff check .` passed.
- `uv run python -m ruff format --check .` failed: 16 files would be
  reformatted.
- `uv run python -m pre_commit run --all-files` failed only at the Ruff format
  check hook for the same formatting debt.

| Review ID | Final status | Commit | Tests / checks | Implementation note | Porting note | Reviewer reply draft |
|---|---|---|---|---|---|---|
| R01 | in_progress | `1728f08` plus pending integration docs | `uv run python -m ruff check .` passed; `uv run python -m ruff format --check .` failed on 16-file formatting debt; `ty` remains unresolved. | Ruff, pre-commit, gitlint, radon, and import-linter were restored. The `ty` decision is intentionally left unresolved instead of guessing a tool policy. | Port the tooling commit before dependency/runtime changes. Decide `ty` separately in the target repo. | Static tooling was restored and now runs. Ruff check passes; Ruff format debt and `ty` remain tracked rather than hidden. |
| R02 | done | `86cbb1a` | `uv lock`, `uv sync --all-extras --dev`, `uv run python -m pytest`, and final `uv run python -m pytest` passed. | Root `pyproject.toml` and `uv.lock` are now the dependency source of truth; requirements files remain compatibility inputs for Docker/ClearML/legacy pip paths. | Port `pyproject.toml` and `uv.lock` together. Re-check lock resolution on the target OS. | Dependency management has been normalized to uv workspace plus lockfile while preserving requirements compatibility. |
| R03 | deferred | pending operational follow-up | Final residual search found `OMP_NUM_THREADS`, `OPENBLAS_NUM_THREADS`, and `MKL_NUM_THREADS` in `clearml/app.py`, `scripts/local_run.py`, and `scripts/make_sample_data.py`. | Thread env defaults still exist in Python entrypoint/helper code. No K8 or runtime environment migration was performed in this cleanup branch. | Move these defaults to the target execution environment, workflow, or ClearML runtime configuration in a separate operational change. | Thread env responsibility was checked. Python-side defaults still remain, so this item is deferred to an operational environment pass. |
| R04 | not_applicable | N/A | No `kubectl`, `kustomize`, `helm`, cluster verification, rollout, or Kubernetes manifest checks were run. | Kubernetes / K8 verification is intentionally out of scope for this repository cleanup. | Exclude this item from target-repo cleanup cherry-picks. Handle cluster evidence in the repo/environment that owns Kubernetes deployment. | 今回のレビュー対応ではKubernetes実機検証は対象外とし、コード基盤整理から除外しました。 |
| R05 | done | `ba51813` | `uv run python -m pytest` passed; final Ruff check passed. | ClearML execution task use was narrowed with Protocol-style typing; broader payload typing remains staged behind typed config and artifact boundaries. | Port with the adapter tests. Do not remove flexible artifact/logger handling without ClearML SDK version checks. | Adapter typing was tightened where safe while preserving ClearML SDK compatibility. |
| R06 | done | `ba51813` | Adapter mapping tests and final pytest passed. | The execution image path uses direct API access with a compatibility fallback only where SDK versions differ. | Keep the fallback in target repos until the ClearML SDK version is confirmed. | Predictable API access replaced the unsafe execution-image `getattr` path. |
| R07 | done | `ba51813` | Stage mapping tests and final pytest passed. | Stage values now pass through `StageName` / `as_stage_name()` while serialized stage strings remain stable. | Port with tests that protect unknown-stage failure behavior. | Stage names now have a typed validation boundary without changing ClearML-visible stage strings. |
| R08 | in_progress | `86cbb1a` | Import probe and final pytest passed; residual search still shows shadow-guard logic. | ClearML SDK import errors are narrower and documented, but the local `clearml/` directory remains for template compatibility. | Do not rename `clearml/` during cherry-pick. A future rename needs replacement template entrypoints. | ClearML SDK import handling is safer, but full shadow elimination is staged because current templates still execute `clearml/app.py` and `clearml/pipelines.py`. |
| R09 | done | `ba51813` | Adapter tests and final pytest passed. | ClearML runtime validation moved to entrypoint-adjacent code; dataset existence is a narrow helper. | Re-run ClearML dry-run in the target repo after profile changes. | ClearML configuration checks are now closer to entrypoints, and lower helpers focus on dataset existence. |
| R10 | done | `ba51813` | Adapter dataset tests and final pytest passed. | Dataset existence accepts a non-empty `str`; optional handling is done by callers. | Confirm target code does not pass `None` into the helper. | Dataset ID handling was narrowed to the actual ClearML API contract. |
| R11 | done | `ba51813` | Mapping compatibility tests and final pytest passed. | `as_str_list()` is the clearer helper; old `as_list()` remains as a compatibility wrapper. | Keep wrapper until target callers have migrated. | The list conversion helper now states that it returns strings, with compatibility preserved. |
| R12 | done | `ba51813` | Mapping serialization tests and final pytest passed. | `_ui_value()` behavior moved to ClearML parameter transport naming with unchanged serialization. | Port with R15 so naming wrappers and tests stay aligned. | The helper now describes ClearML parameter transport rather than display UI behavior. |
| R13 | needs_confirmation | `1728f08` | Local runner availability cannot be verified. | Workflows keep `ubuntu-latest` with TODOs for `arc-runner-set-spdml-ml-pipeline`. | Confirm runner label in the target repo/org before changing `runs-on`. | The runner-set request is preserved as a TODO because availability cannot be proven locally. |
| R14 | done | `1728f08` | Workflow YAML checks, final pytest, and smoke workflow review passed. | CI and smoke workflows are split; smoke uses existing `tabular_pipeline.yaml` and `tabular_infer.yaml`. | Port with R01 and then verify GitHub Actions on the target branch. | Common CI and product smoke checks were restored without referencing removed task configs. |
| R15 | in_progress | `ba51813`, `f4fe03a` | Final residual search still finds compatibility `ui_*` wrappers and tests. | Internal runtime parameter naming was introduced, but public/compat wrappers such as `default_ui_params` and `pipeline_ui_params` remain. | Keep wrappers during target migration; remove only after external imports and tests migrate. | Runtime parameter vocabulary is improved internally. Compatibility wrappers remain intentionally. |
| R16 | in_progress | `86cbb1a` | `uv run python -m ruff check .` passed; residual search shows direct-entrypoint bootstrap remains. | Local scripts use package imports. ClearML direct-entrypoint files retain bootstrap-before-import compatibility with documented lint exceptions. | Keep exceptions until ClearML template entrypoints are changed and remotely verified. | Import-order linting is restored for normal code; remaining direct-entrypoint exceptions are documented compatibility cases. |
| R17 | needs_confirmation | `86cbb1a` | Local script smoke and final pytest passed; residual search still finds `clearml/_entrypoint_bootstrap.py`. | `scripts/_bootstrap.py` was removed. ClearML entrypoint bootstrap remains for direct `clearml/app.py` and `clearml/pipelines.py` execution. | Verify ClearML remote Agent working directory/template execution before deleting the ClearML bootstrap. | Local sys.path bootstrap was removed; ClearML direct-entrypoint bootstrap remains pending remote confirmation. |
| R18 | needs_confirmation | `f4fe03a` | Manifest/domain-plan tests, fake renderer tests, and final pytest passed. | Core contracts, tabular manifest/policy, and ClearML rendering from `DomainPipelinePlan` are implemented. ClearML SDK/UI/remote behavior still needs manual confirmation. | Port R18 before tabular split commits. Run ClearML dry-run, localhost UI, and remote execution in the target environment. | Runtime/package manifest boundary was added and ClearML runtime is closer to a renderer; live ClearML verification remains required. |
| R19 | done | `ba51813` | IO tests and final pytest passed. | `TABLE_SUFFIXES` is applied through common table path checks. | Keep suffix helper centralized when adding table formats in the target repo. | Table file suffix policy is centralized across discovery paths. |
| R20 | done | `ba51813` | Final residual search finds `set_dotted_path` only in docs/source history and a smoke assertion. | The unused non-exported alias was removed from code. | If the target repo has external imports, add a deprecation shim there instead of direct deletion. | The unused alias was removed after repository-wide reference checks. |
| R21 | done | `1728f08` | `uv run python -m pre_commit run --all-files` runs and fails only on Ruff format debt. | `.pre-commit-config.yaml` and `.gitlint` were restored with minimal rules. | Activate the project environment in the target repo before running hooks. | Pre-commit and gitlint are restored; the only current hook failure is tracked formatting debt. |
| R22 | done | `1728f08` | Final status is clean before docs edits; line-ending churn was controlled. | `.gitattributes` was restored with conservative text/binary rules. | Re-check line-ending churn after cherry-pick. | `.gitattributes` is restored to reduce OS line-ending churn and protect binary files. |
| R23 | needs_confirmation | `1728f08` | MkDocs workflow exists; Pages target cannot be verified locally. | MkDocs build/deploy workflow was restored, but GitHub Pages deployment settings remain external. | Confirm Pages settings/environment and branch permissions in the target repo. | Docs workflow is restored; GitHub Pages deployment remains a repository settings confirmation item. |
| R24 | done | `1728f08` | JSON checks passed in Phase 1; final status clean before docs edits. | Shared VS Code settings/extensions were added without personal paths or secrets. | Adjust only team extension preferences in the target repo. | Shared editor settings were restored safely. |
| R25 | done | `1728f08` | JSON checks passed in Phase 1; final status clean before docs edits. | `ml_platform.code-workspace` contains root, `pkgs/core`, and `pkgs/tabular` relative folders. | Update relative folder names only if target layout differs. | Package workspace was restored with portable relative paths. |
| R26 | in_progress | `ba51813` | Config model tests and final pytest passed; residual search still shows downstream `dict[str, Any]` use. | Typed config models and parsing boundary were introduced while preserving dict compatibility. Full downstream rewrite is intentionally staged. | Port typed boundary first, then migrate downstream accessors incrementally. | The external config boundary is now typed and validated, but downstream dict compatibility remains for staged migration. |
| R27 | done | `ba51813` | Final residual search finds `class Registry` only in review source docs. | Unused `ml_platform_core.registry` was deleted and smoke-tested. | Check for target-only imports before deleting; add shim if necessary. | The unused Registry was removed because this repo had no live references. |
| TABULAR-SPLIT | done | `0301d0e`, `b96f622`, `e5eda0a`, `50c607a` | Characterization tests and final pytest passed. | Added characterization coverage, then split plotting, inference, and pipeline/training responsibilities with compatibility facades. | Port characterization tests before any split commits. Keep `plots.py`, `infer.py`, and `pipeline.py` facades until target imports migrate. | Tabular output contracts are characterized and the large modules are split behind compatibility facades. |
