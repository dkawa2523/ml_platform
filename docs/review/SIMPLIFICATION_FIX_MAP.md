# Simplification Fix Map

This map tracks cleanup work separately from PR review IDs. The final Lean
completion pass records what is done in this repository and what still needs
external confirmation before deletion.

Allowed statuses:

- `todo`
- `in_progress`
- `done`
- `needs_confirmation`
- `blocked`
- `deferred`

## Sxx Tickets

| ID | Category | Target files | Problem | Proposed action | Deletion allowed | Risk | Required tests | Status | Commit | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| S01 | confirmed unused code removal | repository-wide Python code | `vulture` is available via `uvx`; it confirmed one unused model-artifact helper path and still reports runner/config false positives. | Delete only candidates confirmed by `vulture`, repo grep, and tests. Keep broad deletion gated by entrypoint/public API review. | Partially. Confirmed dead helpers are removable; public entrypoints and config fields are not. | Medium | `uv run python -m pytest`, import smoke tests, targeted entrypoint checks | needs_confirmation | `f37b856` partial | CLEAN-1 removed one-line wrappers. 2026-06-30 cleanup removed unused model-info loader helpers. Remaining vulture findings are `ModelConfig` fields used as config keys and `run_stage` used by manifest runner paths. |
| S02 | stale compatibility wrapper removal | `clearml/adapter.py`, `clearml/pipelines.py`, `clearml/templates.py`, removed tabular facade modules | Deprecated UI-named wrappers and facade modules remained after review-response migrations. | Confirmed one-line aliases are removed. Tabular runner paths now point to implementation packages and the old tabular facade modules are deleted. ClearML script compatibility remains until remote usage is confirmed. | Partially. Tabular facades and one-line aliases are removed; ClearML script compatibility is not deletion-safe yet. | High | full pytest, `tests/test_clearml_mapping.py`, tabular characterization tests, ClearML dry-run | needs_confirmation | `f37b856`, `de61cdb` | CLEAN-1 removed `as_list`, `default_ui_params`, `grouped_ui_params`, `apply_ui_params`, `pipeline_ui_params`, and `_task_ui_params`. CLEAN-4 removed private facade re-exports. 2026-06-30 cleanup removed `ml_platform_tabular.infer`, `pipeline`, and `plots` after runner migration. |
| S03 | excessive contract/spec simplification | `pkgs/core/src/ml_platform_core/contracts.py`, `pkgs/tabular/src/ml_platform_tabular/manifest.py`, docs | One-implementation runtime Protocols and descriptive-only contract fields added cognitive load without an active runtime consumer. | Removed the unused `runtime_types.py` Protocol scaffold and trimmed manifest contracts to fields used for runner resolution, schema validation, artifact/parameter declarations, and domain plan rendering. | Yes, for the confirmed unused Protocol module and descriptive-only fields. | Medium | `tests/test_runtime_manifest.py`, fake renderer tests, full pytest | done | `b73cd7c` | Final `uv run python -m pytest` and `uv run python -m ruff check .` pass. Remaining manifest/policy size cleanup is future readability work, not S03 completion debt. |
| S04 | diagnostics and error handling simplification | `clearml/adapter.py`, `clearml/reports.py`, tests, docs | Reporting code mixed malformed output files with ClearML logger failures and silently swallowed broad errors. | Centralized JSON/CSV best-effort parsing, limited parse fallbacks to file/encoding/value errors, and stopped silencing non-`TypeError` ClearML logger failures. | Yes, for broad catch blocks and duplicate parsing helpers covered by tests. | Medium | ClearML mapping tests, report behavior tests, compileall, pytest | done | `38c95de` | Operator-facing dry-run/sync `print()` output remains. `clearml_dataset_exists()` keeps a broad SDK existence check because ClearML raises version-specific missing-dataset errors. |
| S05 | file responsibility cleanup after split | `clearml/app.py`, `clearml/pipelines.py`, `clearml/templates.py`, `stage.py`, tabular tests | Split-era callers still used compatibility facades as private helper aggregators, and ClearML entrypoints duplicated bootstrap loading code. | CLEAN-4 moved `stage.py` and tests to implementation modules, shrank tabular facades, and removed duplicated `importlib` bootstrap loaders. | Partially. Private re-exports were removed; broad ClearML/reporting deletion is not allowed without separate coverage. | Medium | full pytest, tabular characterization tests, targeted smoke tests, ClearML mapping tests | done | `de61cdb` | This cleanup clarifies current file ownership without changing runner paths. Remaining large-file work should stay behavior-preserving and narrow in future Sxx work. |
| S06 | dependency and dev-tool pruning | `pyproject.toml`, `uv.lock`, `requirements*.txt`, `.pre-commit-config.yaml`, active setup docs | `deptry` was run via `uvx`, but workspace/local-package and import-name false positives make dependency deletion unconfirmed. | Keep dependencies unchanged until a package-scoped/manual audit proves each candidate unused. Keep active setup commands on `uv sync` / `uv run`. | No dependency deletion until `deptry` findings are triaged against Docker, ClearML, docs, and optional imports. | Medium | `uv lock`, `uv sync --frozen`, `uv run python -m pytest`, pre-commit | needs_confirmation | `8d3b726` | `requirements.txt`, `requirements-dev.txt`, and `requirements-docs.txt` remain for Docker/ClearML/legacy compatibility. `deptry` findings are currently noisy rather than actionable. |
| S07 | obsolete docs and comments cleanup | `README.md`, `docs/CODEX_HANDOFF.md`, `clearml/README.md`, `docs/ml_platform_mkdocs/*` | Active docs contained stale setup commands and old module ownership notes for `pipeline.py`, `infer.py`, and `plots.py`. | CLEAN-5 pruned active docs, replaced old commands with uv commands, and documented current `training`, `inference`, and `plotting` ownership. | Yes, for active docs outside `docs/review/source`. | Low | docs grep, link/path spot checks, compileall, pytest, ruff | done | `8d3b726` | Review evidence docs remain historical. Some review-response docs intentionally mention old states. Final MkDocs strict build passed in CLEAN-5. |
| S08 | ClearML runtime surface simplification | `clearml/_entrypoint_bootstrap.py`, `clearml/app.py`, `clearml/pipelines.py`, `clearml/templates.py`, `clearml/adapter.py` | Direct-entrypoint compatibility and SDK shadow guards keep runtime surface broad. | CLEAN-4 removed duplicated `spec_from_file_location` bootstrap loaders from the three direct entrypoints. Confirm ClearML remote execution before deleting `_entrypoint_bootstrap.py` or renaming `clearml/`. | Only after ClearML remote/template verification. | High | ClearML template dry-run, local script smoke, remote Agent manual verification, full pytest | needs_confirmation | `de61cdb` | Direct entrypoint paths are preserved. `_entrypoint_bootstrap.py`, SDK shadow guard, and script metadata compatibility helpers remain intentionally. |
| S09 | tabular training/inference facade cleanup | deleted `pkgs/tabular/src/ml_platform_tabular/{infer.py,pipeline.py,plots.py}`, `inference/*`, `training/*`, `plotting/*`, `stage.py`, tests | Compatibility facades re-exported private helpers and tests/stage code imported those helpers through old modules. | Moved repo-internal imports and runner paths to implementation packages, then deleted the old tabular facade modules. | Yes, completed for the tabular facades after repo grep, runner path migration, and tests. | Medium | `tests/test_tabular_characterization.py`, `tests/test_infer_schema_check.py`, `tests/test_stage_smoke.py`, full pytest | done | `de61cdb` | Current runner paths are `ml_platform_tabular.inference:run_infer` and `ml_platform_tabular.training:run_pipeline`. Plot helpers are exposed from `ml_platform_tabular.plotting`. The unused `EvaluationResult.to_dict()` compatibility wrapper was also removed. |
| S10 | final lean validation and porting notes | docs and repository-wide checks | Cleanup needs a final pass to prove no behavior or porting contracts regressed. | Re-ran checks, updated porting notes, and recorded residual failures explicitly. | N/A | Low | compileall, pytest, ruff check, ruff format check, radon, optional vulture/deptry | done | pending final commit | Completion is `pass_with_notes`: `uv` compileall/pytest/ruff check pass; ruff format, bare `python`, vulture, deptry, and ClearML remote checks remain documented notes. Kubernetes / K8 remains excluded. |

## Final Lean Completion Status

| ID | Final status | Tests / checks | Remaining note | Porting note |
|---|---|---|---|---|
| S01 | needs_confirmation | Final pytest and ruff check pass; `vulture` runs via `uvx`. | Remaining findings are config-field and runner-path false positives. Do not delete broad unused candidates without target import proof. | Re-run `vulture` in the target repo before deleting more code. |
| S02 | needs_confirmation | Mapping, characterization, full pytest, and ruff check pass. | Tabular facades are removed; ClearML script compatibility remains. | Port tabular runner path migration with facade deletion. Keep ClearML direct-entrypoint compatibility unless target remote templates are verified. |
| S03 | done | Runtime manifest tests and final full checks pass via `uv run python`. | None for current scope. | Port after review response commits and before facade cleanup. |
| S04 | done | ClearML mapping tests and final full checks pass via `uv run python`. | Broad ClearML missing-dataset catch remains intentionally. | Preserve ClearML SDK compatibility behavior unless SDK version is fixed. |
| S05 | done | Targeted facade tests and final full checks pass via `uv run python`. | Larger ClearML/reporting splits are optional future cleanup. | Port with S08/S09 so import paths and entrypoint behavior stay aligned. |
| S06 | needs_confirmation | Final pytest and ruff check pass; `deptry` runs via `uvx` but is noisy for this workspace. | No dependency was removed. | Keep requirements compatibility files until target Docker/ClearML/docs paths are confirmed. |
| S07 | done | MkDocs strict build passed in CLEAN-5; final full checks pass via `uv run python`. | Historical review docs intentionally keep old wording. | Port active docs cleanup after dependency/uv setup. |
| S08 | needs_confirmation | Full pytest and ruff check pass; ClearML remote not verified. | `_entrypoint_bootstrap.py`, SDK shadow guard, and script metadata compatibility remain. | Verify ClearML remote Agent/template execution before deleting runtime compatibility files. |
| S09 | done | Targeted tabular tests, characterization tests, and final full checks pass. | Public tabular facades have been removed. | Port runner paths and tests to `ml_platform_tabular.inference`, `training`, and `plotting` before deleting old modules in a target repo. |
| S10 | done | Final `uv` compileall, pytest, ruff check, radon, and residual searches completed. | Completion is `pass_with_notes`. | Cherry-pick final completion docs after all Lean cleanup commits. |

## First Recommended Cleanup Batch

1. S06: triage package-scoped `deptry` findings before dependency deletion.
2. S08: keep ClearML script compatibility until remote-template confirmation.
3. S05: reduce ClearML/reporting and tabular evaluation complexity in small
   behavior-preserving commits.
4. S10: run final validation and update porting notes.
