# Compatibility file for Docker/ClearML remote paths.
# Source of truth is pyproject.toml plus uv.lock; keep this file in sync while
# deploy/base/Dockerfile and ClearML Agent setup still consume requirements.txt.
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
scikit-learn>=1.3
pillow>=10.0
clearml==2.1.7
