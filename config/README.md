# config

Configuration uses two axes:

```text
tasks/     what to run
profiles/  where to run
```

Task values override profile values. CLI overrides are applied last.

Keep this layout until there is a clear product need for more structure.
