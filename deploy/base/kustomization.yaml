apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - controller.yaml
  - worker.yaml
# secret.example.yaml is intentionally not listed as a resource.
# Copy it to secret.yaml or create the Secret by your cluster secret manager.
